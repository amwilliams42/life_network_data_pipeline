lan_dbt:
  target: prod
  outputs:
    prod:
      type: postgres
      host: warehouse
      user: "{{ env_var('WAREHOUSE_USER') }}"
      password: "{{ env_var('WAREHOUSE_PASS') }}"
      port: 5432
      dbname: warehouse
      threads: 8
      schema: warehouse

