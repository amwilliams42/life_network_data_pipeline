# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: life_network_data_pipeline
prefect-version: 3.4.7

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    id: clone-step
    repository: https://github.com/amwilliams42/life_network_data_pipeline.git
    branch: master
    access_token: "{{ prefect.blocks.secret.github-pat }}"
- prefect.deployments.steps.run_shell_script:
    script: |
      echo "Post-clone: Setting up secrets..."
      
      # Check if source secrets file exists
      if [ ! -f "/host-secrets/secrets.toml" ]; then
        echo "ERROR: Secrets file not found at /host-secrets/secrets.toml"
        exit 1
      fi
      
      # Create .dlt directory
      mkdir -p .dlt
      
      # Copy secrets file
      cp /host-secrets/secrets.toml .dlt/secrets.toml
      chmod 600 .dlt/secrets.toml
      
      echo "✅ Secrets file copied successfully"
      echo "Working directory: $(pwd)"
      echo "Secrets file size: $(stat -c%s .dlt/secrets.toml) bytes"
- prefect.deployments.steps.pip_install_requirements:
    directory: "{{ clone-step.directory }}"
    requirements_file: requirements.txt
    stream_output: False

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: Test Deployment
  schedule:
    cron: "*/10 * * * *"
  entrypoint: hello_world.py:hello_world
  parameters:
    name: Deployment! TEST
  work_pool:
    name: pipeline_workflow
    work_queue_name: pipeline_workflow
- name: TN Pull CAD Trips
  description: Pull new and updated CAD trips for TN and MS every 5 minutes
  schedule:
    interval: 300
  parameters:
    dataset_name: traumasoft_tn
    source_name: tn_database
  entrypoint: flows/cad_import.py:load_cad_trips
  work_pool:
    name: pipeline_workflow
- name: TN Daily Pull
  schedule:
    cron: "0 2 * * *"
  entrypoint: flows/daily_import.py:daily_import
  parameters:
    dataset_name: traumasoft_tn
    source_name: tn_database
  work_pool:
    name: pipeline_workflow